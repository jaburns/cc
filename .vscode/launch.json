{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/hello",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "buildTask",
        },
    ]
}