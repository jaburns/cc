{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/sdlvk_app",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "buildTask",
            "envFile": "${workspaceFolder}/.vscode/.env"
        }
    ]
}